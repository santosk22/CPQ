trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.12'
    addToPath: true

- script: |
    python --version
    python PyCharmMiscProject/welcome.py
  displayName: 'Run welcome.py'
